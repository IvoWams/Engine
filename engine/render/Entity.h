#ifndef ENGINE_RENDER_ENTITY_H
#define ENGINE_RENDER_ENTITY_H

namespace engine::render
{
    struct Entity
    {
        // This should contain base information that all the renderers can use

        int id;


    };
}

#endif